# Система работы с макетом дополнительных контролей

Веб-приложение для работы с макетом дополнительных контролей из файла Template.xml, включающее графический интерфейс для просмотра, фильтрации и создания векторной базы данных.

## Возможности

- 📋 **Просмотр списка контролей** с пагинацией и сортировкой
- 🔍 **Фильтрация контролей** по различным критериям (идентификатор, наименование, URI, обязательность и др.)
- 📄 **Детальное описание** каждого контроля со всеми характеристиками
- 🗄️ **Создание векторной БД** (ChromaDB) на основе макета для семантического поиска

## Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

## Первая загрузка на GitHub

Если вы впервые загружаете проект на GitHub, см. подробную инструкцию: **[GITHUB_UPLOAD_GUIDE.md](GITHUB_UPLOAD_GUIDE.md)**

**⚠️ Если у вас нет Git:**
- Сначала установите Git: см. **[INSTALL_GIT.md](INSTALL_GIT.md)**
- Или используйте вариант без Git LFS: **[QUICK_START_NO_LFS.md](QUICK_START_NO_LFS.md)**

**Быстрая проверка перед загрузкой:**
```powershell
.\check_repo_structure.ps1
```

## Установка

1. Клонируйте репозиторий или скачайте проект

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

1. **Для администратора:** Убедитесь, что файл `Template.xml` находится в корневой директории проекта

2. Запустите приложение:
```bash
python run.py
```

Или напрямую через Streamlit:
```bash
streamlit run src/main.py
```

3. Приложение откроется в браузере по адресу `http://localhost:8501`

## Использование

### Для пользователя

Приложение автоматически загружает данные из файла `Template.xml` при запуске. Пользователю не требуется выполнять какие-либо действия по загрузке файла - просто откройте приложение и начните работать с контролями.

### Для администратора

**Настройка файла макета:**
- Поместите файл `Template.xml` в корневую директорию проекта
- При обновлении макета замените файл `Template.xml` на новый
- После замены файла перезапустите приложение для загрузки новых данных

**Создание векторной БД:**
- Функция создания векторной БД доступна только администратору
- Для доступа к этой функции обратитесь к разработчику

### Работа с фильтрами

- Используйте боковую панель для настройки фильтров
- Фильтры применяются автоматически при изменении значений
- Используйте кнопку "Сбросить" для очистки всех фильтров

### Просмотр списка контролей

- Вкладка "Список контролей" показывает таблицу всех контролей
- Используйте быстрый поиск для фильтрации по таблице
- Выберите строку в таблице для просмотра деталей
- Используйте пагинацию для навигации по большому количеству контролей

### Просмотр описания контроля

- Вкладка "Описание контроля" показывает детальную информацию о выбранном контроле
- Используйте кнопки "Предыдущий" и "Следующий" для навигации между контролями
- Алгоритм отображается с подсветкой синтаксиса

### Создание векторной БД

> **Примечание:** Функция создания векторной БД доступна только администратору. Для обычных пользователей эта функция скрыта.

## Структура проекта

```
DK/
├── Template.xml              # Исходный макет (XML файл)
├── requirements.txt          # Зависимости Python
├── README.md                 # Документация
├── src/
│   ├── main.py              # Точка входа Streamlit приложения
│   ├── parser/
│   │   └── xml_parser.py    # Парсер XML
│   ├── models/
│   │   └── control.py       # Модель данных контроля
│   ├── vector_db/
│   │   ├── embeddings.py     # Генерация эмбеддингов
│   │   └── chroma_manager.py # Работа с ChromaDB
│   └── gui/
│       ├── filters.py        # Компоненты фильтров
│       ├── list_view.py      # Компонент списка
│       ├── details_view.py   # Компонент описания
│       └── commands.py       # Команды (создание БД)
└── chroma_db/               # Директория для ChromaDB (создается автоматически)
```

## Технологии

- **Streamlit** - веб-фреймворк для создания интерфейса
- **ChromaDB** - векторная база данных
- **sentence-transformers** - библиотека для создания эмбеддингов
- **pandas** - работа с данными
- **lxml** - парсинг XML

## Модель эмбеддингов

По умолчанию используется модель `paraphrase-multilingual-MiniLM-L12-v2`, которая:
- Поддерживает русский язык
- Оптимизирована для работы с ChromaDB
- Работает локально (не требует API ключей)
- Имеет хорошее качество для семантического поиска

## Публикация приложения

Чтобы сделать приложение доступным для других людей через браузер, есть несколько вариантов:

### 🚀 Быстрый способ: Streamlit Community Cloud (Рекомендуется)

1. **Создайте репозиторий на GitHub** и загрузите туда весь код (включая `Template.xml`)
2. **Зарегистрируйтесь** на https://share.streamlit.io/ через GitHub
3. **Нажмите "New app"** и укажите:
   - Репозиторий: ваш репозиторий
   - Main file path: `src/main.py`
4. **Нажмите "Deploy"** - через несколько минут приложение будет доступно по URL вида `https://your-app-name.streamlit.app`

**Подробные инструкции:** см. файл `DEPLOYMENT.md`

### Обновление приложения

После внесения изменений в код:
1. Загрузите изменения на GitHub: `git add .`, `git commit -m "..."`, `git push`
2. Streamlit автоматически обновит приложение через 1-2 минуты

**Быстрое обновление:** используйте скрипт `update.ps1`:
```powershell
.\update.ps1 "Описание изменений"
```

**Подробная инструкция:** см. файл `UPDATE_APP.md`

### Другие варианты

- **Docker** - используйте `Dockerfile` для создания образа
- **VPS/Сервер** - запустите на любом сервере с Python
- **Heroku** - используйте `Procfile` для деплоя

Подробности в файле `DEPLOYMENT.md`.

## Примечания

- При первом запуске модель эмбеддингов будет загружена автоматически (может занять некоторое время)
- Векторная БД сохраняется локально в директории `chroma_db`
- Для больших файлов создание векторной БД может занять несколько минут
- ⚠️ **Важно для публикации:** Убедитесь, что файл `Template.xml` находится в репозитории

## Лицензия

Проект создан для внутреннего использования.

